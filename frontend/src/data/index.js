const listGoldar = require("./goldar");
const listKota = require("./kota-kab");
const listTipeDonor = require("./tipe-donor");

const listData = {
    listGoldar,
    listKota,
    listTipeDonor,
};

module.exports = listData;
